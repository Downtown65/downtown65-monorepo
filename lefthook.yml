commit-msg:
  jobs:
    - name: commitlint
      run: pnpm exec commitlint --edit {1}

pre-push:
  parallel: true
  jobs:
    - name: lint
      run: pnpm biome check .

    - name: typecheck
      run: pnpm tsc --build

    - name: knip
      run: pnpm knip
