{
  "name": "@dt65/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "typegen": "varlock typegen",
    "test": "vitest run --config vitest.config.ts",
    "lint": "biome lint",
    "format": "biome check --write",
    "ci:lint": "biome ci",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@dt65/shared": "workspace:*",
    "@varlock/infisical-plugin": "^0.0.3",
    "@varlock/vite-integration": "^0.2.3",
    "drizzle-orm": "catalog:",
    "hono": "4.7.5",
    "varlock": "^0.4.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.27.0",
    "@cloudflare/vitest-pool-workers": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "drizzle-kit": "catalog:",
    "rimraf": "catalog:",
    "typescript": "catalog:",
    "vite": "^7.3.1",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  }
}
